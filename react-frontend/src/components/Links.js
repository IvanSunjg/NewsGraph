import * as d3 from "d3";

function RenderLinks(linkContainerRef, svgLefts, svgRights, templateData, leftNo, rightNo) {

    const leftData = templateData[leftNo];
    const rightDta = templateData[rightNo];
    console.log(leftData);
    console.log(rightDta);
    
}

export default RenderLinks;